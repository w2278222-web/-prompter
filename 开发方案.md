# 安卓提词器 App 开发方案

## 需求确认

| 项目 | 选择 |
|------|------|
| 平台 | Android only |
| 语音来源 | 实时语音识别 (Android SpeechRecognizer，需联网) |
| 脚本输入 | txt + docx 文档导入 |
| 滚屏控制 | 完全自动，读到哪里滚到哪里 |
| 显示效果 | 逐行滚动，高亮行居中放大 |
| 屏幕方向 | 竖屏 + 横屏 |
| 字号 | 用户可调基础字号 |
| 滚动节奏 | 逐句滚动 |

---

## 技术栈

- **语言**: Kotlin
- **UI**: Jetpack Compose
- **语音识别**: Android SpeechRecognizer API
- **文档解析**: Apache POI (docx) + 内置文本解析
- **架构**: MVVM + Clean Architecture

---

## 核心模块

### 1. 文档导入模块
- 支持 .txt 和 .docx 文件
- 解析后存储为纯文本列表（按句子/行分割）

### 2. 语音识别模块
- 调用 Android SpeechRecognizer
- 实时接收识别结果
- 通过字符串相似度算法与脚本匹配，找到当前朗读位置

### 3. 滚动显示模块
- LazyColumn 实现列表滚动
- 当前行：高亮(颜色/背景) + 放大(1.3x) + 居中
- 其他行：正常字号
- 滚动动画平滑

### 4. 设置模块
- 基础字号调节滑块
- 屏幕方向切换

---

## 数据流

```
用户朗读 → SpeechRecognizer识别文字 → 与脚本句子模糊匹配 → 确定当前行索引 → 滚动到该行并放大高亮
```

---

## 文件结构

```
app/
├── data/           # 文档解析、存储
├── domain/         # 核心业务逻辑（匹配算法、滚动控制）
├── presentation/   # Compose UI + ViewModel
└── speech/         # 语音识别封装
```

---

## 关键技术点

1. **语音-文字匹配**: 使用编辑距离(Levenshtein)算法进行模糊匹配，应对识别误差
2. **滚动居中**: `LazyColumn.scrollToItem` + `itemOffset` 精确定位到视口中央
3. **文字放大**: Compose `FontScale` 或 `TextStyle` 局部缩放
